# 基于文章提炼的技术面试准备问答

## 说明

- 本文档基于以下可提取文章整理：
  - [腾讯客户端一面](https://blog.csdn.net/tinouq/article/details/159995543)
  - [C++ Qt面试题 | 精选25题](https://blog.csdn.net/m0_73443478/article/details/143335393)
- 另外两篇链接当前未能稳定抓取正文，如果你后面把正文贴给我，我可以继续补齐：
  - [链接1](https://blog.csdn.net/tinouq/article/details/159315701?spm=1001.2014.3001.5502)
  - [链接2](https://blog.csdn.net/tinouq/article/details/159757202?spm=1001.2014.3001.5502)
- 这份问答已经按你的方向做了定制，重点偏 `C++ / Qt / Linux / 网络 / 多线程 / IM项目`。

---

## 一、C++ 基础高频题

### 1. 说一下函数传参的区别，值传递、指针传递、引用传递有什么区别？

**标准回答：**
值传递会拷贝一份参数副本，安全但对大对象开销大；指针传递本质上传地址，可以修改原对象，也能表达“可为空”；引用传递本质上是别名，语法更自然，也避免了空指针问题。工程里如果是大对象只读，我通常优先用 `const T&`；如果参数可选，用指针；如果需要直接修改并且语义明确，用引用。

**结合你自己项目可以这么补一句：**
在我的 IM 项目里，如果传输的是较大的消息对象或配置对象，我会优先考虑常量引用，避免不必要拷贝。

### 2. 函数指针在什么情况下会用到？

**标准回答：**
函数指针常见场景有四类。第一是回调函数，比如异步任务完成后通知调用方；第二是状态机或分发表，可以根据消息类型或协议号快速路由；第三是在 C 风格接口里模拟多态；第四是动态加载库时，通过函数地址调用模块功能。

**面试加分说法：**
在即时通讯或者网络服务里，消息类型分发是一个很典型场景。如果不用大量 `if-else`，就可以考虑函数表或更现代的映射分发方式。

### 3. 你遇到过函数指针导致内存泄漏吗？

**标准回答：**
函数指针本身不直接导致内存泄漏，但它经常和资源管理问题一起出现。比如回调上下文对象生命周期没管理好、动态加载库后没有释放句柄，或者 Lambda 捕获了堆对象却没有正确释放。规避方法还是回到 RAII，让资源和对象生命周期绑定。

### 4. 为什么有时候用回调函数，不用接口？

**标准回答：**
回调函数更轻量，耦合更低，性能开销也更小，适合简单通知场景。接口更适合复杂行为抽象，扩展性更好，但设计和维护成本更高。简单说，逻辑单一、强调轻量和性能时更偏回调；行为复杂、需要统一抽象时更偏接口。

---

## 二、多线程与并发

### 5. 生产者消费者问题，不使用锁如何实现？

**标准回答：**
如果是单生产者单消费者，可以用无锁环形队列；如果是多生产者多消费者，一般要基于原子操作和 CAS 来实现无锁队列。无锁方案性能高，但实现复杂，还要处理 ABA 问题、内存序和缓存一致性问题。

**更稳妥的面试说法：**
我知道无锁队列的基本思路，但工程里我会优先根据复杂度和稳定性做权衡，不会为了无锁而无锁。如果并发量没有极端到必须无锁，锁方案通常更稳。

### 6. 同步怎么实现？

**标准回答：**
常见同步手段包括互斥锁、条件变量、信号量、读写锁和原子变量。互斥锁适合保护临界区；条件变量适合线程间等待和通知；读写锁适合读多写少；原子变量适合轻量状态同步。选型要看共享资源特点和性能要求。

### 7. 你项目里怎么做线程同步？

**标准回答：**
在我的 IM 项目里，连接映射、房间映射和用户在线状态都属于共享数据结构，所以我用了 `std::mutex` 和 `std::lock_guard` 来保护访问。这样虽然不是最激进的性能方案，但实现清晰，也能先保证正确性。

### 8. 什么是 ABA 问题？

**标准回答：**
ABA 问题是 CAS 里一个经典问题。一个值从 A 变成 B 又变回 A，CAS 看到还是 A，就误以为中间没发生变化。常见解决办法是加版本号，比如用带版本的指针或双字 CAS。

---

## 三、网络与即时通讯

### 9. 如何把消息从服务端发送到客户端？

**标准回答：**
实时场景主流方案是 WebSocket，因为它支持长连接和双向通信。除此之外还有 SSE、长轮询，以及移动端推送服务。像聊天系统这种实时性要求高、消息频繁的场景，WebSocket 最合适。

**结合你的项目可以这样答：**
我的聊天室项目就是用 WebSocket 做消息下发，服务端维护用户和连接的映射，再根据房间或用户 ID 做广播和定向推送。

### 10. 如何维持心跳消息？

**标准回答：**
心跳一般分两层。协议层要设计 `ping/pong` 或类似字段；策略层要定义发送间隔、超时时间和断线清理机制。客户端通常主动发心跳，服务端记录最后心跳时间，超过阈值就判定连接失效并清理资源。

### 11. 为什么聊天系统要做心跳？

**标准回答：**
因为 TCP 连接断开不一定能被应用层立刻感知，尤其在弱网或移动网络场景下更明显。心跳能帮助我们及时发现死连接，维护在线状态，也能避免服务端长期持有无效连接。

### 12. WebSocket、SSE、长轮询有什么区别？

**标准回答：**
WebSocket 是全双工长连接，最适合实时双向通信；SSE 是服务端单向推送，适合通知流；长轮询本质还是 HTTP，只是不断重建请求，兼容性高但开销更大。聊天系统通常优先 WebSocket。

### 13. 海量长连接的核心挑战是什么？

**标准回答：**
核心挑战有连接管理、心跳清理、消息路由、在线状态同步和资源占用控制。单机内存映射适合当前实例快速路由，但如果要多实例扩展，还要把全局状态同步到 Redis 或其他中间件。

---

## 四、Qt 高频题

### 14. Qt 常用的五大模块有哪些？

**标准回答：**
常用模块包括 `QtCore`、`QtGui`、`QtWidgets`、`QtNetwork` 和 `QtSql`。如果面试问到你项目里用到了什么，你可以重点说 `QtCore`、`QtWidgets` 和 `Qt Charts`。

### 15. 什么是 Qt 的信号槽机制？

**标准回答：**
信号槽是 Qt 里对象间通信的机制。一个对象发出信号，另一个对象的槽函数收到后执行对应逻辑。它的核心价值是解耦，让界面事件和业务逻辑之间不直接绑死。

**结合你的项目可以这样答：**
我在 Qt 正交试验工具里，很多按钮点击、分析触发和界面更新，本质上都是通过 Qt 的事件和信号槽机制来串起来的。

### 16. 信号槽是同步的还是异步的？

**标准回答：**
要看连接方式。`DirectConnection` 是同步调用，类似普通函数调用；`QueuedConnection` 是异步，会把调用投递到目标线程事件队列；默认的 `AutoConnection` 会根据线程关系自动选择。

### 17. 为什么不建议把业务逻辑全写在 UI 控件里？

**标准回答：**
因为 UI 层更适合负责展示和交互，业务逻辑混在控件里会让代码耦合太高、难测也难维护。更合理的做法是让界面负责触发，核心逻辑放到独立模块里。

**这题要注意：**
你的正交项目现在主窗口里逻辑比较多，所以回答时可以说“我也意识到这一点，所以后面已经开始把导出和分析逻辑往独立模块拆”。

### 18. QPainter 是什么？用在哪些场景？

**标准回答：**
`QPainter` 是 Qt 的绘图引擎，用于在控件、窗口或图片上绘制图形、文字和自定义界面元素。适合自定义控件、图形渲染、图表绘制等场景。

### 19. QTimer 和 QBasicTimer 有什么区别？

**标准回答：**
`QTimer` 更高级，使用方便，适合大多数定时任务；`QBasicTimer` 更轻量，通常配合 `timerEvent()` 使用，适合需要更细粒度控制的场景。

### 20. QThread 和 QtConcurrent 有什么区别？

**标准回答：**
`QThread` 适合需要明确线程对象和生命周期管理的场景，控制更细；`QtConcurrent` 更偏任务并行，使用简单，但灵活性稍弱。如果是后台简单任务可以用 `QtConcurrent`，如果需要更细的线程控制就用 `QThread`。

### 21. QML 和 Qt Widgets 的区别是什么？

**标准回答：**
`Qt Widgets` 更适合传统桌面工具，控件体系成熟；`QML` 更适合动态界面和更现代的交互效果。你的项目是桌面分析工具，所以选择 `Qt Widgets` 是合理的。

### 22. Qt 中如何做数据库操作？

**标准回答：**
常用类有 `QSqlDatabase`、`QSqlQuery`、`QSqlTableModel` 等。实际开发里一般先建立连接，再执行 SQL，再读取结果集。如果面试官追问，你可以顺带提一下事务、连接管理和错误处理。

---

## 五、结合你项目的定制追问

### 23. 你的 Qt 项目为什么不用 QML？

**标准回答：**
因为这个项目本质是一个偏工具型的桌面应用，重点在表格数据录入、分析结果展示和简单图表，而不是复杂动画或高动态界面。`Qt Widgets` 在这类场景下开发效率更高，也更符合项目需求。

### 24. 你的 IM 项目里为什么要维护 `user -> connection set`？

**标准回答：**
这是为了支持多设备登录和多端同步。单个用户可能同时在多个设备上线，如果只维护一个连接，会导致消息同步能力不足。用连接集合后，可以给同一用户所有在线端一起推送。

### 25. 你的 IM 项目里为什么先写 Redis 再写 MySQL？

**标准回答：**
因为聊天链路更重视实时性。先写 Redis 可以快速完成缓存和在线分发，再异步落 MySQL 做持久化。这样响应快，但我也知道它会带来一致性和失败重试的问题，后续如果继续优化，我会考虑线程池或任务队列。

### 26. 你的 Qt 项目如果重构，最先改哪里？

**标准回答：**
我会优先把主窗口里的分析逻辑抽出来，按“表型配置、分析算法、界面展示”拆分。因为现在逻辑主要集中在主窗口里，能跑通，但后续扩展新表型和维护会越来越吃力。

### 27. 你的项目里体现了哪些 C++ 能力？

**标准回答：**
Qt 项目体现的是用 C++ 做数据结构组织、算法实现和模块封装；IM 项目体现的是用 C++ 做连接管理、并发控制、协议设计和系统逻辑实现。一个偏桌面工具，一个偏实时服务端，刚好覆盖了我在 C++ 上两类比较典型的实践。

---

## 六、建议优先背诵的 10 题

- 函数传参区别
- 函数指针使用场景
- 无锁生产者消费者怎么实现
- 同步方式有哪些
- WebSocket 为什么适合聊天
- 心跳机制怎么设计
- 信号槽机制是什么
- QThread 和 QtConcurrent 区别
- 为什么你的 Qt 项目选 Widgets
- 为什么你的 IM 项目先写 Redis 再写 MySQL

---

## 七、下一步最适合继续补的内容

- 把这份问答压缩成“面试口语版”
- 单独做一份“腾讯客户端 / C++ Qt 岗位模拟问答”
- 把你两个项目的源码追问进一步细化到类和函数级别

---

## 八、补充自新抓取的两篇文章

### 来源说明

- `百格医疗一面`：已确认主题重点是 `Qt 事件循环 / 事件驱动 / 串口和界面响应`，当前以下问题基于抓取到的标题与摘要整理。
- `大湾区国创中心面经C++`：已抓取到较完整正文目录与问题点，新增题主要来自这篇。

## 九、Qt 事件驱动补充题

### 28. Qt 程序的运行核心是什么？什么是事件循环？

**标准回答：**
Qt 程序的运行核心是事件循环。应用启动后，`QApplication::exec()` 会进入事件循环，持续等待并分发各种事件，比如鼠标点击、键盘输入、定时器触发、串口数据到达、网络消息到达等。对象收到事件后，再通过 `event()`、具体事件处理函数，或者信号槽机制完成响应。

### 29. 什么是事件驱动模型？

**标准回答：**
事件驱动模型指的是程序平时不主动线性执行所有逻辑，而是等待事件发生后再响应。GUI、网络、串口这类场景天然适合事件驱动，因为外部输入本来就是异步到来的。

### 30. Qt 的事件机制和信号槽机制有什么区别？

**标准回答：**
事件机制更底层，主要处理鼠标、键盘、窗口、定时器、串口等系统或框架事件；信号槽更偏对象之间通信，用来解耦业务逻辑。可以理解成事件告诉你“发生了什么”，信号槽帮助对象之间协作“接下来怎么做”。

### 31. 串口收到数据后，Qt 程序一般怎么处理？

**标准回答：**
通常是串口对象在收到数据时触发类似 `readyRead()` 的信号，然后槽函数里读取串口缓冲区数据，再做解析和界面更新。这样做不需要自己一直轮询，符合 Qt 的事件驱动模式。

### 32. 为什么不建议在主线程里做耗时的串口解析或复杂计算？

**标准回答：**
因为主线程负责事件循环和界面刷新，如果在主线程里做耗时任务，事件循环会被阻塞，界面就会卡顿或无响应。正确做法一般是把耗时任务放到工作线程，再把结果通过信号槽发回主线程更新界面。

## 十、从“大湾区国创中心面经C++”补充的题

### 33. `vector` 和 `list` 的区别是什么？什么时候才考虑 `list`？

**标准回答：**
`vector` 底层是连续内存，随机访问快、缓存友好、尾插效率高；`list` 底层是链表，插入删除方便，但随机访问慢，而且内存开销更大。工程里默认优先 `vector`，只有在中间频繁插删、元素很大、不想频繁移动元素，或者特别在意节点地址稳定时，才考虑 `list`。

### 34. 说一下 lambda 表达式和常见捕获方式

**标准回答：**
lambda 是匿名函数对象，常用于回调、算法和短逻辑封装。常见捕获方式有：`[]` 不捕获，`[=]` 值捕获，`[&]` 引用捕获，`[this]` 捕获当前对象指针。值捕获安全但有拷贝成本；引用捕获高效，但要特别注意外部变量生命周期。

### 35. 什么是虚函数表？它怎么支持多态？

**标准回答：**
有虚函数的类会有虚函数表 `vtable`，对象内部会有虚表指针 `vptr`。当通过基类指针调用虚函数时，程序不是直接按指针静态类型调用，而是通过对象里的 `vptr` 找到实际类对应的函数地址，所以最终能调用到派生类重写后的实现。

### 36. 为什么析构函数通常要声明为虚函数？

**标准回答：**
如果基类析构不是虚函数，通过基类指针 `delete` 派生类对象时，只会执行基类析构，派生类自己持有的资源可能就释放不了，最终造成资源泄漏。所以只要类有可能被继承并通过基类指针释放，就应该把析构函数写成虚函数。

### 37. 智能指针有哪些？`weak_ptr` 的核心作用是什么？

**标准回答：**
常见智能指针有 `unique_ptr`、`shared_ptr` 和 `weak_ptr`。`unique_ptr` 表示独占所有权，默认首选；`shared_ptr` 表示共享所有权，内部有引用计数；`weak_ptr` 不拥有对象，不增加引用计数，主要用来解决 `shared_ptr` 的循环引用问题，也适合做观察者。

### 38. 说一下 MVC、工厂模式、单例模式

**标准回答：**
MVC 是职责分离思想，把数据、界面和控制逻辑分开；工厂模式是把对象创建过程封装起来，降低调用方和具体类耦合；单例模式是保证某个类只有一个实例，常见于配置管理或日志模块。但单例不应该滥用，否则会引入全局状态和测试困难。

### 39. 互斥锁和读写锁有什么区别？

**标准回答：**
互斥锁是完全独占的，同一时刻只有一个线程能访问；读写锁允许多个读线程同时访问，但写线程必须独占。读多写少时读写锁更有优势，但它实现更复杂，也可能出现写线程长期等待的问题，所以要根据场景选。

### 40. Git 冲突一般怎么处理？

**标准回答：**
处理 Git 冲突时，我会先明确双方修改意图，而不是机械地选保留哪一边。具体流程通常是：定位冲突块、理解两边逻辑、手动合并、重新编译或测试，再提交。关键不是把冲突标记删掉，而是确保最终逻辑正确且一致。

## 十一、更新后的优先背诵题

- Qt 事件循环是什么
- Qt 事件机制和信号槽的区别
- 为什么 GUI 主线程不能做耗时操作
- `vector` 和 `list` 的区别
- lambda 捕获方式
- 虚函数表和多态
- 智能指针和 `weak_ptr`
- 互斥锁和读写锁区别
- 为什么你的 Qt 项目选 Widgets
- 为什么你的 IM 项目先写 Redis 再写 MySQL
