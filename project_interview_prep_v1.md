# 简历项目面试准备（第一版）

## 使用说明

- 这份文档基于你提供的两个项目代码整理，目标是帮助你准备项目面试。
- 结构分为：项目概述、面试高频题、推荐回答思路、风险提醒。
- 建议你先背“项目概述”和“高频题前 5 题”，再补细节。

---

## 项目一：OrthogonalExperimentAssistant

### 1. 项目概述

- 项目类型：基于 `Qt Widgets` 的桌面数据分析工具。
- 核心目标：支持多种正交表的数据录入，并完成极差分析、方差分析、因素分析和图表展示。
- 主要技术：`C++`、`Qt`、`Qt Widgets`、`Qt Charts`、`QTableWidget`、CSV 导出。
- 你可以重点强调的工作：
  - 设计不同正交表类型的数据录入界面。
  - 根据正交表规则实现极差分析和方差分析计算流程。
  - 用 `Qt Charts` 把极差和方差结果做成可视化柱状图。
  - 实现 CSV 导出，便于结果保存和二次处理。

### 2. 你在面试里可以这样介绍

这个项目是一个基于 Qt 的桌面端正交试验分析工具，主要解决的是实验数据录入、统计计算和可视化展示的问题。项目支持多种正交表类型，用户录入实验结果后，可以执行极差分析、方差分析和因素分析，并通过柱状图直观看到不同因素的影响程度。我主要做了表格数据处理、分析逻辑实现、图表展示和 CSV 导出这些核心功能。

### 3. 高频问题与回答思路

#### Q1：这个项目解决了什么问题？

答题思路：
它主要解决正交试验数据人工计算麻烦、容易出错的问题。以前用户要手工做极差分析和方差分析，现在可以在桌面应用里直接录入数据、自动计算并生成图表，提高效率和准确性。

#### Q2：为什么选择 Qt 来做？

答题思路：
这个项目是典型桌面端工具，Qt 在桌面 GUI 开发、表格控件、信号槽和图表集成方面比较成熟。尤其是 `QTableWidget` 适合做数据录入界面，`Qt Charts` 适合快速完成分析结果可视化，所以我选择了 Qt。

#### Q3：项目支持哪些正交表类型？

答题思路：
项目里支持多种正交表，比如 `L9(3^4)`、`L4(2^3)`、`L8(2^7)`、`L12(2^11)`、`L16(4^5)`、`L25(5^6)`。这些类型的区别主要在于试验行数、因素数和水平数不同，所以表格结构和计算逻辑也会不一样。

#### Q4：你是怎么组织不同正交表的？

答题思路：
整体思路是根据不同表型动态创建对应大小的 `QTableWidget`，然后在分析阶段按表型走不同的计算逻辑。核心差异在于行列数不同、每个因素在不同水平上的组合关系不同，所以计算 `K` 值、均值、极差和 `SS` 时公式会随表型变化。

#### Q5：极差分析是怎么做的？

答题思路：
极差分析的核心是先按因素和水平对实验结果分组求和，得到每个水平下的 `K` 值；然后再除以该水平出现次数，得到均值 `k`；最后取最大均值减最小均值，得到极差 `R`。极差越大，说明该因素对结果影响越明显。

#### Q6：方差分析在项目里怎么实现？

答题思路：
项目里是根据正交分析的常见计算方法，先求不同水平结果和的平方项，再结合总和修正项计算各因素的离差平方和 `SS`。最后把这些结果追加到表格中，用于和极差分析一起做因素比较。

#### Q7：因素分析为什么要做图表？

答题思路：
因为只看表格数字不够直观。图表可以把不同因素在“极差分析”和“方差分析”两个维度的结果并排展示，用户能快速看出哪个因素影响更大。这个项目里我用的是 `Qt Charts` 的柱状图。

#### Q8：CSV 导出是怎么做的？

答题思路：
我封装了一个 `DataExporter` 模块，遍历表头和每一行单元格内容，把它们按 CSV 格式写到文件里。为了避免导出后格式错乱，还做了字段转义处理，比如字段里有逗号、引号、换行时会自动加引号并转义。

#### Q9：项目里最难的地方是什么？

答题思路：
最难的是不同正交表类型对应的计算逻辑不一样，不能简单复用一套公式。我要先理清每种表型的水平分布规则，再把计算过程落到代码里。另外分析结果要同步反映到 UI 表格和图表中，所以数据、界面和业务逻辑要保持一致。

#### Q10：你在这个项目里学到了什么？

答题思路：
我对 Qt 桌面开发流程更熟悉了，尤其是信号槽、表格控件和图表控件的使用。另外也锻炼了把统计分析流程翻译成程序逻辑的能力，包括如何组织数据、如何把计算结果回填到界面。

### 4. 面试官可能继续追问的细节

- 为什么用 `QTableWidget`，不用 `QTableView + Model`？
- 你怎么校验用户输入的数据是否合法？
- 图表数据和表格数据如何保持一致？
- 如果以后要支持更多正交表类型，你会怎么重构？
- 如果数据量更大，这种实现有哪些性能问题？

### 5. 推荐回答升级版

#### 为什么用 `QTableWidget`？

因为这个项目规模不大，数据录入和展示逻辑都比较直接，`QTableWidget` 上手更快，能快速完成原型和功能验证。如果后续要支持更复杂的数据模型、更高性能或更强可扩展性，我会考虑切到 `Model/View` 架构。

#### 如果以后重构，你会怎么做？

我会把“表型配置”“分析算法”“UI 展示”拆开。比如用统一配置描述正交表结构，把极差分析和方差分析抽象成独立算法模块，再让界面只负责展示和交互。这样可维护性会更好，也更方便新增表型。

### 6. 风险提醒

- 从代码上看，`mainwindow.cpp` 里承担了大量业务逻辑，面试时不要把项目讲成“已经完全模块化设计”。
- `orthogonalanalyzer.cpp` 当前更像重构中的框架，不要说成“所有分析逻辑都已经完整抽到了分析类里”。
- 更稳妥的说法是：你已经开始把分析逻辑和导出逻辑往独立模块拆分，其中 `DataExporter` 已经是独立模块，分析类也做了初步抽象。

---

## 项目二：im_app

### 1. 项目概述

- 项目类型：即时通讯系统。
- 核心目标：实现用户间实时消息通信，以及房间、在线状态、消息持久化等能力。
- 主要技术：`C++17`、`Drogon`、`WebSocket`、`MySQL`、`Redis`、`Docker`。
- 从代码上能明确看到的重点：
  - WebSocket 连接建立、鉴权参数校验、连接生命周期管理。
  - 使用 `mutex + lock_guard` 保护连接映射和房间映射。
  - 基于消息协议对象做消息解析和分发。
  - Redis 做在线状态、设备集合、消息缓存。
  - MySQL 做异步持久化。

### 2. 你在面试里可以这样介绍

这个项目是一个即时通讯系统，后端用 C++ 和 Drogon 开发，核心通信方式是 WebSocket。项目重点在于实时消息处理、连接管理、房间广播、在线状态维护以及 Redis 和 MySQL 的组合存储。我主要参与了 WebSocket 控制器、消息协议处理、线程安全的连接管理，以及消息缓存和持久化这部分逻辑。

### 3. 高频问题与回答思路

#### Q1：为什么聊天系统要用 WebSocket？

答题思路：
聊天场景强调实时性和双向通信，如果用 HTTP 轮询，开销大而且延迟高。WebSocket 建立长连接后，服务端可以主动推送消息，更适合私聊、群聊、在线状态同步这些场景。

#### Q2：连接建立时你做了什么？

答题思路：
连接建立时，我先校验请求和连接对象是否有效，然后从请求参数里取 `user_id` 和 `token`。如果参数缺失，就返回错误并关闭连接；如果通过，就把连接和用户建立映射，同时记录心跳时间，并把在线状态同步到 Redis。

#### Q3：为什么要维护连接和用户的映射关系？

答题思路：
因为后续要做定向发消息、房间广播、在线状态统计和断连清理，所以必须能快速从“连接找到用户”、也能从“用户找到连接集合”。这样既能支持单设备，也能支持多设备登录。

#### Q4：多设备登录是怎么支持的？

答题思路：
项目里不是把一个用户只绑定到一个连接，而是绑定到一个连接集合。也就是说 `user -> set<connection>`。这样同一个用户在多个设备上登录时，服务端可以给这个用户的所有在线设备推送消息。

#### Q5：为什么这里要加锁？

答题思路：
因为 WebSocket 连接、房间成员、用户在线映射都属于共享数据结构，可能会被多个线程同时访问。为了避免竞态条件和脏数据，我用 `std::mutex` 和 `std::lock_guard` 来保护这些 map 和 set 的读写。

#### Q6：消息协议是怎么设计的？

答题思路：
我把不同类型的 WebSocket 消息抽象成统一基类，然后派生出文本消息、心跳消息、加入房间、离开房间、输入状态等不同类型。收到消息后先解析 `type` 字段，再分发给对应的处理函数，这样协议更清晰，也更方便扩展。

#### Q7：文本消息的处理流程是什么？

答题思路：
收到文本消息后，先校验内容是否为空，再判断当前连接是否已经加入对应房间。校验通过后，先把消息写入 Redis，用于快速访问和状态维护；然后异步持久化到 MySQL；最后把消息广播给房间内其他连接。

#### Q8：为什么 Redis 和 MySQL 要一起用？

答题思路：
Redis 更适合做高频读写、在线状态和临时消息缓存，响应快；MySQL 更适合做可靠持久化和历史数据存储。两者结合可以兼顾实时性和数据可靠性。

#### Q9：心跳机制是怎么做的？

答题思路：
客户端发送心跳 `ping` 后，服务端回复 `pong`，同时更新该连接的最后心跳时间。这样可以判断连接是否还活着，为后续超时清理和在线状态维护提供依据。

#### Q10：断开连接时服务端要做什么？

答题思路：
断开连接后，要把这个连接从多个映射结构里清理掉，包括连接到用户的映射、连接到房间的映射、用户到连接集合的映射，以及 Redis 里的设备集合。如果该用户所有设备都离线了，还要把在线状态清掉。

#### Q11：房间广播怎么实现？

答题思路：
项目里维护了 `room -> set<connection>` 的映射。广播时先找到目标房间的连接集合，再遍历给每个在线连接发送消息。对于“输入中”这种场景，还支持排除发送者，只广播给其他用户。

#### Q12：你觉得这个项目最难的点是什么？

答题思路：
我觉得最难的是连接生命周期管理和并发安全。因为消息收发、房间变更、心跳更新、断开连接这些操作会交错发生，如果映射关系没维护好，很容易出现消息发错、状态不一致或者内存/连接泄漏的问题。

### 4. 面试官可能继续追问的细节

- token 校验是怎么做的，为什么放在 URL 参数里？
- 为什么 Redis 在线状态设置了过期时间？
- 为什么消息先写 Redis 再异步写 MySQL？
- `detach` 一个持久化线程有什么问题？
- 房间成员关系除了内存 map，还为什么要在 Redis 里存一份？
- 如果服务重启，内存里的连接映射怎么办？

### 5. 推荐回答升级版

#### 为什么先写 Redis 再写 MySQL？

聊天系统更看重发送链路的实时性，所以先把消息放到 Redis，可以更快完成在线分发和短期缓存，再异步落到 MySQL 做持久化。这样做的好处是响应更快，但也会带来一致性设计的问题，所以后续如果继续完善，我会补失败重试或消息确认机制。

#### `detach` 持久化线程有没有风险？

有风险。优点是实现简单、不会阻塞主消息链路；风险是线程数不可控，而且失败重试、异常追踪和资源管理都不够完善。如果继续优化，我会考虑线程池、任务队列或异步持久化服务。

#### 为什么还要维护 Redis 里的用户设备集合？

内存结构更适合当前进程内快速路由，但 Redis 更适合做跨进程共享状态、在线统计和故障恢复辅助。把设备集合写到 Redis，可以为以后做分布式部署或多实例扩展打基础。

### 6. 风险提醒

- 从代码上看，项目里已经有比较完整的 WebSocket 消息处理和 Redis/MySQL 配合逻辑，这部分你可以放心讲。
- 但 `chat_db_ops.h` 里有不少数据库操作仍是骨架和占位，不要说成“所有数据库业务都已经完整落地”。
- 更稳妥的说法是：你重点完成了实时通信链路、连接管理、消息协议和缓存/持久化主流程，数据库辅助模块还在持续完善。

---

## 两个项目的对比讲法

### 面试官问：你两个项目分别体现了什么能力？

推荐回答：
第一个项目更偏桌面端工具开发，体现的是 Qt GUI、表格数据处理、算法落地和可视化能力；第二个项目更偏服务端通信系统，体现的是 WebSocket 实时通信、并发安全、协议设计和 Redis/MySQL 架构能力。一个偏客户端工具，一个偏后端实时系统，刚好让我在 C++ 的不同方向上都做了比较完整的实践。

---

## 面试前重点背诵

- 正交项目：
  - 为什么用 Qt。
  - 极差分析和方差分析怎么做。
  - 为什么要做图表展示。
  - 如果重构你会怎么拆模块。

- 聊天室项目：
  - 为什么用 WebSocket。
  - 连接建立和断开时做了什么。
  - 为什么要用 `mutex + lock_guard`。
  - Redis 和 MySQL 分别负责什么。
  - 文本消息的完整处理链路是什么。

---

## 下一步建议

- 你可以先拿这份文档练项目口述，每个项目分别练 `1 分钟版` 和 `3 分钟版`。
- 如果你愿意，我下一步可以继续帮你做：
  - `项目 1 分钟 / 3 分钟介绍稿`
  - `项目追问标准答案版`
  - `根据代码细节整理“面试官可能挑刺的问题”`

---

## 项目追问标准答案版

这一部分更偏“面试现场可直接说”的表达。你不需要逐字背，但建议把句式和逻辑记住。

## 一、OrthogonalExperimentAssistant 标准答案

### 1. 如果面试官问：这个项目你具体负责什么？

标准回答：
这个项目里我主要负责三块内容。第一块是表格数据处理，也就是根据不同正交表类型动态创建数据录入表格。第二块是分析逻辑实现，包括极差分析、方差分析和因素分析相关结果的计算。第三块是结果展示和导出，我用 Qt Charts 做了柱状图展示，也封装了 CSV 导出功能，方便保存分析结果。

### 2. 如果面试官问：你为什么做这个项目？

标准回答：
一方面是想提升自己在 Qt 桌面开发上的能力，另一方面这个题目本身也比较有实际意义。正交试验分析如果手工做，计算过程比较繁琐，而且容易出错，所以我就想把数据录入、计算和可视化整合到一个桌面工具里。

### 3. 如果面试官问：极差分析怎么讲给非专业的人听？

标准回答：
可以把它理解成比较“哪个因素影响更大”的一种直观方法。对于每个因素，我会先统计它在不同水平下对应的实验结果，然后算出每个水平的平均表现。最后看这个因素在不同水平下的最大值和最小值差了多少，这个差值就是极差。差值越大，说明这个因素越关键。

### 4. 如果面试官问：方差分析和极差分析有什么区别？

标准回答：
极差分析更偏直观比较，计算简单，适合快速判断因素影响大小。方差分析更偏统计意义上的分析，它会基于离差平方和来衡量因素贡献，理论上更严谨一些。所以在项目里我把两种分析都做了，再结合图表展示，用户能从两个维度看因素影响。

### 5. 如果面试官问：为什么要把结果回填到表格里？

标准回答：
因为这个项目是一个分析工具，用户除了看最终结论，还会关心中间过程，比如 `K` 值、均值、极差和 `SS`。把这些结果直接追加到表格里，一方面方便核对计算过程，另一方面也有利于后续导出和复查。

### 6. 如果面试官问：这个项目里你最满意的点是什么？

标准回答：
我最满意的是把“数据录入、分析计算、图表展示、结果导出”串成了一个完整流程。它不只是一个静态界面，而是用户录完数据后真的能完成分析并得到结果，这让我对 Qt 应用开发和算法落地都有了更完整的实践。

### 7. 如果面试官问：如果让你继续优化，你会先改什么？

标准回答：
我会优先做模块拆分。因为目前主窗口里承担了比较多业务逻辑，后续我会把不同表型配置、分析算法和界面展示进一步拆开。这样新增新的正交表类型时，不需要在主窗口里继续堆逻辑，维护性会更好。

### 8. 如果面试官问：这个项目有哪些不足？

标准回答：
目前它的不足主要有两点。第一，业务逻辑还比较集中在主窗口类里，可扩展性一般。第二，不同正交表类型的计算逻辑虽然能跑通，但复用程度还不够高。如果继续完善，我会进一步抽象统一的数据模型和分析接口。

### 9. 如果面试官问：为什么说这个项目体现了你的 C++ 能力？

标准回答：
这个项目虽然是 Qt 桌面应用，但本质上我还是用 C++ 来完成数据结构组织、分析流程实现和模块封装。尤其是把统计分析逻辑落成代码、让界面和数据处理协同工作，这里面体现了我对 C++ 工程组织和业务实现的能力。

### 10. 如果面试官问：你从这个项目里最大的成长是什么？

标准回答：
最大的成长是我开始能把一个实际问题拆成“界面、数据、算法、结果展示”几个部分来做，而不是只停留在写某个小功能。另外我也更熟悉 Qt 的信号槽、表格控件和图表控件了。

## 二、im_app 标准答案

### 1. 如果面试官问：你在这个 IM 项目里主要做了什么？

标准回答：
我主要做的是后端实时通信链路这一块。包括 WebSocket 连接建立和关闭时的状态维护、消息协议解析和分发、房间广播、用户多设备连接管理，以及 Redis 缓存和 MySQL 持久化这部分逻辑。

### 2. 如果面试官问：这个项目的核心难点是什么？

标准回答：
我觉得核心难点有两个。第一个是连接生命周期管理，因为连接建立、发消息、进房间、退房间、断开连接这些动作是交错发生的，要保证状态一致。第二个是并发安全，因为多个线程可能同时操作连接映射和房间映射，所以共享数据结构必须加锁保护。

### 3. 如果面试官问：你的消息处理链路是怎样的？

标准回答：
整体链路是这样的：客户端通过 WebSocket 发消息过来，服务端先解析 JSON 协议，识别 `type` 字段，然后分发到对应的处理函数。以文本消息为例，会先校验消息内容和房间权限，再写入 Redis 做缓存和状态维护，然后异步持久化到 MySQL，最后广播给房间内的其他在线连接。

### 4. 如果面试官问：为什么你要自己设计消息协议？

标准回答：
因为聊天系统里消息类型不只一种，不只是文本，还有心跳、加入房间、离开房间、输入状态等。如果没有统一协议，后续扩展会很乱。所以我把消息抽象成统一基类，再按不同 `type` 做派生和分发，这样协议清晰，后面加新类型也更方便。

### 5. 如果面试官问：你怎么理解 Redis 和 MySQL 的分工？

标准回答：
我的理解是 Redis 更像高速层，适合做在线状态、设备集合和消息缓存，因为它读写快；MySQL 更像持久层，适合做历史数据保存和可靠存储。这个项目里我把两者结合起来，就是希望兼顾实时性和可追溯性。

### 6. 如果面试官问：为什么要维护用户到连接集合，而不是单个连接？

标准回答：
因为实际聊天系统里用户可能会在多个设备上同时登录，比如电脑端和手机端。如果只存一个连接，就没法支持多设备同步消息。所以我在服务端维护的是 `user -> set<connection>`，这样可以给同一个用户的多个在线设备一起推送。

### 7. 如果面试官问：你是怎么做线程安全的？

标准回答：
我主要是用 `std::mutex` 和 `std::lock_guard` 来保护共享数据结构，比如连接和用户的映射、房间和连接的映射、心跳记录这些。这样做的目的是避免多个线程同时修改这些结构时出现竞态条件。

### 8. 如果面试官问：你觉得现在的实现还有哪些风险？

标准回答：
目前最大的风险点是异步持久化这块我用了 `detach` 线程，优点是简单、不阻塞主链路，但缺点是线程数不可控，失败重试和异常治理也不够完善。如果继续优化，我会考虑线程池或者任务队列来替代。

### 9. 如果面试官问：为什么连接关闭时要清理那么多映射？

标准回答：
因为一个连接在系统里会同时参与多个关系，比如它属于某个用户、可能在多个房间里，也会有心跳记录和设备记录。如果关闭时只删一部分，剩下的脏状态会影响在线判断、消息路由甚至造成资源泄漏，所以断连清理必须做完整。

### 10. 如果面试官问：如果以后做分布式，你觉得哪些地方要改？

标准回答：
如果做分布式，单机内存里的连接映射只能解决当前实例内的问题，跨实例的在线状态和消息路由需要更多依赖 Redis 或消息队列。也就是说，本地内存更适合做当前实例的快速路由，而全局状态需要放到可共享的中间层里。

### 11. 如果面试官问：你在这个项目里学到了什么？

标准回答：
这个项目让我更理解实时系统和普通 CRUD 项目的区别。它不仅要考虑功能，还要考虑连接状态、协议设计、并发访问和消息链路。对我来说，最大的收获是把 C++ 用在了更偏服务端和实时通信的场景里。

### 12. 如果面试官问：如果让你再做一次，你会怎么优化？

标准回答：
我会从三个方向优化。第一是把持久化改成更稳定的异步任务模型，比如线程池或消息队列。第二是把鉴权和连接管理再做得更规范一些，比如把 token 校验链路抽得更清楚。第三是继续补全数据库层和异常处理，让整个系统更接近可上线形态。

## 三、两个项目通用追问标准答案

### 1. 如果面试官问：你在项目里是如何定位问题的？

标准回答：
我的习惯是先复现问题，再缩小范围。比如先判断是输入数据问题、业务逻辑问题，还是状态同步问题；然后再结合日志、关键变量和调用链去定位。能稳定复现的问题，我会优先把复现路径固定下来，再去改。

### 2. 如果面试官问：你在项目里遇到 bug 怎么处理？

标准回答：
我一般先确认 bug 的触发条件和影响范围，然后看它是单点逻辑错误，还是设计层面的状态不一致。如果是单点问题，我会先快速修复；如果是结构性问题，我会考虑是不是需要顺手做一点重构，避免以后反复踩坑。

### 3. 如果面试官问：你最想体现给面试官的能力是什么？

标准回答：
我希望体现的是两点。第一，我不是只会写零散功能，而是能把一个完整的小系统做起来。第二，我会关注实现之外的工程问题，比如数据结构组织、模块边界、并发安全和后续扩展性。

### 4. 如果面试官问：你写项目时更关注什么？

标准回答：
我会先保证功能能跑通，然后再看结构是不是清晰、状态是不是一致、后续是否方便扩展。因为我觉得项目不只是“写出来”，还要考虑别人能不能接着维护，以及自己以后回来看会不会难受。

## 四、面试时的表达建议

- 讲项目时尽量用“问题-方案-实现-结果”四步。
- 不要一上来陷进代码细节，先把项目价值和整体流程讲清楚。
- 如果某个模块还没完全做完，就说“这部分我已经完成主流程，后续还计划继续完善”，不要硬说成完全成熟。
- 遇到不会的深挖题，可以先说当前实现，再补一句“如果继续优化，我会考虑……”，这样会比直接卡住更好。
