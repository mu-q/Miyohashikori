# C++ 实习面试基础知识题库（第一版）

## 1. C++ 基础

- Q: `new/delete` 和 `malloc/free` 有什么区别？
  A: `new/delete` 会调用构造和析构函数，`malloc/free` 只负责分配和释放内存。

- Q: `const` 有什么作用？
  A: 用来限制修改，常见于修饰变量、指针、函数参数和成员函数。

- Q: 什么是 RAII？
  A: 资源获取即初始化，用对象生命周期管理资源，避免泄漏。

- Q: 智能指针有哪些？
  A: `unique_ptr` 独占，`shared_ptr` 共享，`weak_ptr` 用于解决循环引用。

- Q: 拷贝构造和赋值运算符有什么区别？
  A: 拷贝构造是创建新对象，赋值运算符是给已有对象重新赋值。

- Q: 什么是多态？
  A: 同一接口表现出不同实现，通常通过虚函数实现运行时多态。

- Q: 虚函数表是什么？
  A: 存放虚函数地址的表，用于运行时动态绑定。

## 2. STL / 数据结构

- Q: `vector` 和 `list` 的区别？
  A: `vector` 连续内存，随机访问快；`list` 插入删除快，但访问慢。

- Q: `map` 和 `unordered_map` 的区别？
  A: `map` 基于红黑树，有序；`unordered_map` 基于哈希表，平均查找更快。

- Q: `stack` 和 `queue` 适合什么场景？
  A: `stack` 常用于递归、回溯；`queue` 常用于 BFS、任务调度。

- Q: 哈希冲突怎么解决？
  A: 常见方法有链地址法和开放定址法。

## 3. Linux / 操作系统

- Q: 进程和线程有什么区别？
  A: 进程是资源分配单位，线程是调度单位，同进程线程共享地址空间。

- Q: 什么是死锁？
  A: 多个线程互相等待对方资源，导致都无法继续执行。

- Q: 如何避免死锁？
  A: 统一加锁顺序、减少锁粒度、使用超时锁、避免嵌套锁。

- Q: 进程间通信方式有哪些？
  A: 管道、消息队列、共享内存、信号量、socket。

- Q: `gdb` 常用命令有哪些？
  A: `break`、`run`、`next`、`step`、`print`、`bt`、`watch`。

## 4. 网络 / Socket

- Q: TCP 和 UDP 的区别？
  A: TCP 面向连接、可靠；UDP 无连接、开销小、速度快。

- Q: TCP 三次握手过程是什么？
  A: 客户端发 `SYN`，服务端回 `SYN+ACK`，客户端再回 `ACK`。

- Q: 什么是 `epoll`？
  A: Linux 下高效的 I/O 多路复用机制，适合高并发连接。

- Q: 阻塞、非阻塞、同步、异步怎么理解？
  A: 阻塞看是否等待结果；同步异步看结果通知方式。

- Q: 为什么 `WebSocket` 适合聊天室？
  A: 支持长连接和全双工通信，实时性更好。

## 5. Qt 基础

- Q: Qt 信号槽机制是什么？
  A: 对象间通信机制，发送信号时自动调用对应槽函数。

- Q: Qt 事件循环是什么？
  A: 负责接收和分发事件，是界面响应的核心。

- Q: `Qt Widgets` 和 `Qt Quick` 有什么区别？
  A: `Widgets` 更适合传统桌面开发，`Quick` 更适合动态界面。

- Q: 为什么 GUI 里不能做耗时操作？
  A: 会阻塞主线程，导致界面卡顿或无响应。

## 6. MySQL / 数据库

- Q: 索引有什么作用？
  A: 提高查询速度，但会增加写入和维护成本。

- Q: 什么是事务？
  A: 保证一组操作要么都成功，要么都失败。

- Q: 事务的 ACID 是什么？
  A: 原子性、一致性、隔离性、持久性。

- Q: 为什么要做 SQL 优化？
  A: 减少全表扫描，提升响应速度，降低数据库压力。

## 7. 项目追问常见方向

- Q: 你做项目时最难的问题是什么？
  A: 重点讲“问题是什么、怎么定位、如何解决、结果如何”。

- Q: 为什么这样设计架构？
  A: 重点讲“性能、可维护性、扩展性、复杂度”四个维度。

- Q: 如果让你重做一次，你会怎么改？
  A: 可以从模块拆分、接口抽象、日志、测试、性能优化这几个方向回答。

- Q: 你在项目里负责了什么？
  A: 用“我负责什么 + 怎么做 + 有什么结果”来回答。

## 8. 复习建议

- 先把每题答案压缩成 1 到 2 句。
- 每个知识点都要能结合自己的项目举例。
- 重点背 `C++ / Qt / Linux / 网络 / MySQL / 多线程`。

